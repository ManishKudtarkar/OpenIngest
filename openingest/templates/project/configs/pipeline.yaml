# ─────────────────────────────────────────────────────────────────────────────
# OpenIngest Pipeline Configuration
# ─────────────────────────────────────────────────────────────────────────────

# Cron schedule used by both the built-in scheduler and the Airflow DAG.
# Presets: @hourly | @daily | @midnight | @weekly | @monthly
# Or any 5-field cron expression: "0 6 * * 1-5"
cron: "0 0 * * *"
schedule: "@daily"

# ─────────────────────────────────────────────────────────────────────────────
# Notifications (optional — uncomment and set env vars to enable)
# ─────────────────────────────────────────────────────────────────────────────

# notifications:
#   slack:
#     webhook: ${SLACK_WEBHOOK_URL}
#     on: [success, failure]
#     retry_count: 3
#     retry_delay: 2
#
#   email:
#     smtp_host: smtp.gmail.com
#     smtp_port: 587
#     username: ${EMAIL_USERNAME}
#     password: ${EMAIL_PASSWORD}
#     from: data-pipeline@company.com
#     to:
#       - data-team@company.com
#     on: [failure]
#     retry_count: 2
